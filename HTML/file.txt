grid layout using two properties 
----------------------------------
grid-templete-area
grid-area

// multithreading 
 1- synchronisation and asynchronization 
   synchronization mean when two or more thread used single resource or object 
   one by one sequencially called as synchronization.
-------------------------------------------------------------
2- asynchronization
    asynchronization mean when two more thred used single resource or object 
    simultaneusly called as asynchronization

    notyfy() this method is used for recall single thread at a time from waiting 
    - call a waited thread but only one at a time 
    notyfyAll() this method is used for recall all the thread at a time from waiting
    - call all waited thread at a time 
